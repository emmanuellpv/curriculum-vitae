import { Bachelor } from '@/components/education';

const Education = () => {
  return (
    <section className="page-section">
      <div className="default-container">
        <Bachelor />
      </div>
    </section>
  );
};

export default Education;
